package languagelearner;

public class LanguageLearner {
    
    /**
     * @param args the command line arguments
     */
    //LanguageLearner: Starting point of the Application
    public static void main(String[] args) {
        MainFrame main = new MainFrame(); //Instance of MainFrame, initalizing the GUI and starting the app
    }
}
